---
"@owllayer/vue": minor
---

Add optional `hitl.labels` (`title`, `message`, `approve`, `deny`, `deniedMessage`, `toolLabels`) to `OwlLayerPlugin` to customize the built-in `ApprovalModal` and `ApprovalBanner`; current texts remain the defaults and `useApproval()` exposes the labels (#91).
