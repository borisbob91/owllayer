---
"@owllayer/svelte": minor
---

Add optional `hitl.labels` (`title`, `message`, `approve`, `deny`, `deniedMessage`, `toolLabels`) to `initOwlLayer` to customize the built-in `ApprovalModal` and `ApprovalBanner`; current texts remain the defaults and the new `hitlLabels` store exposes the labels (#92).
