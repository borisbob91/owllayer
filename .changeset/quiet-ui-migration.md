---
'@owllayer/ui': minor
'@domos/ui': patch
---

The canonical UI package is now published as `@owllayer/ui`. The new `@domos/ui` package is a compatibility shim that re-exports the root, dashboard, and devtools surfaces so existing imports continue to work while consumers migrate.
