// TODO Sprint 4 — Checkout tool: initiate_checkout (redirects to /checkout)
// Sprint 5+: in-chat PaymentWidget (Shopify Pay / Apple Pay / Google Pay)

import type { DomOSShopifyConfig } from '../types.js';

export function registerCheckoutTools(_domos: unknown, _config: DomOSShopifyConfig): void {
  // Sprint 4
}
