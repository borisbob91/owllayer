// TODO Sprint 3 — Navigation tools: navigate_to_product, navigate_to_collection
// Simple window.location redirects — registered via registerTool (no data-domos-tool needed)

export function registerNavigationTools(_domos: unknown): void {
  // Sprint 3
}
