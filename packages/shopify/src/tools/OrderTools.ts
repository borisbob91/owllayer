// TODO Sprint 4 — Order tool: get_order_status
// Uses Shopify Storefront API — customer.orders query
// _client is StorefrontClient | null — injected from DomOSShopify.init() (same instance as ProductTools)

export function registerOrderTools(_domos: unknown, _client: unknown): void {
  // Sprint 4
}
