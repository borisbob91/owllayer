// TODO Sprint 2 — Cart tools: add_to_cart, update_cart_item, remove_cart_item, get_cart
// Uses WooCommerce Store API v1: POST /cart/add-item, PUT /cart/items/{key}, DELETE /cart/items/{key}

import type { StoreApiClient } from '../api/StoreApiClient.js';

export function registerCartTools(_domos: unknown, _api: StoreApiClient): void {
  // Sprint 2
}
